{{! Si el usuario está logueado}}
{{#if user}}
<link rel="stylesheet" href="/css/keys.css">
<style>
  @import
  url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@1,900&display=swap');
</style>
   <div class="col-md-12" style="margin-bottom: 5px; ">
        <div class="card text-center border border-primary">
            <h3 class="card-title text-uppercase" style="color: rgb(44, 142, 253);">
                Cuadrangular de los equipo
            </h3>
        </div>
    </div>
  <div id="input"></div>
    <div id="inputBox">
        <option value="selecionar equipo XD">Selecciona el primer equipo clasificado</option>
        <select id="country" name="country" class="btn btn-secondary col-12" required>
        <option value="selecionar equipo XD">Primer equipo clasificado</option>
        {{#each teams}}
                <option value="{{name}}">{{name}}</option>
        {{/each}}        
        </select>
        <option value="selecionar equipo XD">Selecciona el segundo equipo clasificado</option>
        <select id="country" name="country" class="btn btn-secondary col-12" required>
         <option value="selecionar equipo XD">Segundo equipo clasificado </option>
        {{#each teams}}
                <option value="{{name}}">{{name}}</option>
        {{/each}}        
        </select>
    </div>
  </div>
  {{else}}
    <h1>logueate papu XD</h1>
    {{/if}}